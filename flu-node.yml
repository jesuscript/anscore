- hosts:
    - validator
    - signer
  sudo: yes
  roles:
    - role: common
    - role: flu-node
    - { role: flu-service,
        flu_args: "--jsonrpc --config {{flu_config_path}}" }
  handlers:
    - include: handlers/main.yml