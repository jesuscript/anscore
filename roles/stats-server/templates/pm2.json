{
  "apps": [{
    "name": "stats",
    "script": "bin/www",
    "port": 3000,
    "log_date_format": "YYYY-MM-DD HH:mm Z",
    "watch": false,
    "cwd": "{{install_dir}}",
    "env": {
      "WS_SECRET": "{{ ws_secret }}"
    }
  }]
}
